mixin section(name, arr)
	if name == ''
		each item in arr
			input(class='checkboxSquare' type="checkbox" name="")
			label(class='l') Размножить
	if name == 'checkboxSmall'
		each item in arr
			if item.label
				label.checkboxSmallLabel
					input(class='checkboxSmall' type="checkbox" name="")
					span=item.label
			else 
				input(class='checkboxSmall' type="checkbox" name="")